#!/bin/sh
#
# Run specs with great speed and daring-do.
#

if [ -f zeus.json ]; then
  zeus test $@
elif grep rspec Gemfile ; then
  if [ -f bin/rspec ]; then
    ./bin/rspec $@
  else
    bundle exec rspec $@
  fi
else
  if [ -f bin/m ]; then
    ./bin/m $@
  else
    bundle exec m $@
  fi
fi

